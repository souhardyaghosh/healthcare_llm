# M01 — Checkpoints Log

## Block 0 Checkpoint — Initial Inspection / Foundation Rules
- **Status**: PASS WITH NOTES
- **Summary**: Inspected clean repository with Git initialized (`main` branch pointing to `souhardyaghosh/healthcare_llm`).

## Block 1 Checkpoint — Repository + Root Foundation
- **Status**: PASS
- **Summary**: Established base directory layout (`frontend/`, `backend/`, `docs/`), root config (`.gitignore`, `.env.example`, `README.md`, `PROJECT_STATE.md`), and M01 documentation logs.
